# Live site URL (no trailing slash) — used for Open Graph / canonical links.
# Must be a domain that resolves. Until custom DNS is live, use the Vercel URL:
VITE_SITE_URL=https://portfolio-m38t.vercel.app
